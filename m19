Question 24: You have two buttons A & B . When you click on the A button the image will be shown and you click on the B button then your name, phone number, and email will be shown. How will you achieve this problem statement?

Hint 1:- Add the flag to show the different-2 data on click of buttons.

Hint 2:- Add text in the column for showing the name, phone number, and email.

Hint 3:- Add imageView to show the image.

Hint 4:- Add the image to the assets folder.
source code:
import 'package:flutter/material.dart';

class ShowDataApp extends StatefulWidget {
  @override
  _ShowDataAppState createState() => _ShowDataAppState();
}

class _ShowDataAppState extends State<ShowDataApp> {
  // Flag to control what to show: true = show image, false = show info
  bool showImage = true;

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Show Image or Info'),
      ),
      body: Column(
        mainAxisAlignment: MainAxisAlignment.center,
        children: [
          // Buttons Row
          Row(
            mainAxisAlignment: MainAxisAlignment.center,
            children: [
              ElevatedButton(
                onPressed: () {
                  setState(() {
                    showImage = true;
                  });
                },
                child: Text('Button A'),
              ),
              SizedBox(width: 20),
              ElevatedButton(
                onPressed: () {
                  setState(() {
                    showImage = false;
                  });
                },
                child: Text('Button B'),
              ),
            ],
          ),

          SizedBox(height: 40),

          // Show Image or Text based on flag
          showImage
              ? Image.asset(
                  'assets/your_image.png', // Make sure image is in assets folder & declared
                  width: 200,
                  height: 200,
                )
              : Column(
                  children: [
                    Text('Name: John Doe', style: TextStyle(fontSize: 20)),
                    SizedBox(height: 10),
                    Text('Phone: 123-456-7890', style: TextStyle(fontSize: 20)),
                    SizedBox(height: 10),
                    Text('Email: john.doe@example.com', style: TextStyle(fontSize: 20)),
                  ],
                ),
        ],
      ),
    );
  }
}
