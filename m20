Question 06: Build a Flutter widget that displays two text elements in two separate columns. The first text element should say "First Name" and the second should say "Last Name". Make sure the two text elements are evenly spaced and centered within their respective columns. 
source code:
import 'package:flutter/material.dart';

class TwoColumnTextWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center( // Center the Row vertically and horizontally in the screen
        child: Row(
          mainAxisAlignment: MainAxisAlignment.spaceEvenly, // Evenly space columns horizontally
          children: [
            // First Column
            Column(
              mainAxisAlignment: MainAxisAlignment.center, // Center text vertically in the column
              children: [
                Text(
                  'First Name',
                  style: TextStyle(fontSize: 18),
                ),
              ],
            ),

            // Second Column
            Column(
              mainAxisAlignment: MainAxisAlignment.center, // Center text vertically in the column
              children: [
                Text(
                  'Last Name',
                  style: TextStyle(fontSize: 18),
                ),
              ],
            ),
          ],
        ),
      ),
    );
  }
}
