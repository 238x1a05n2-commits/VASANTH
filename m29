Question 5: How to move a widget from the top left corner to the right bottom corner by clicking on a button?

Hint 1:- Use an image widget or container.

Hint 2:- Use Align widget for moving.
source code
import 'package:flutter/material.dart';

class MoveWidgetScreen extends StatefulWidget {
  @override
  _MoveWidgetScreenState createState() => _MoveWidgetScreenState();
}

class _MoveWidgetScreenState extends State<MoveWidgetScreen> {
  Alignment _alignment = Alignment.topLeft;

  void _moveWidget() {
    setState(() {
      // Toggle alignment between topLeft and bottomRight
      _alignment = _alignment == Alignment.topLeft ? Alignment.bottomRight : Alignment.topLeft;
    });
  }

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Move Widget Example'),
      ),
      body: Stack(
        children: [
          AnimatedAlign(
            alignment: _alignment,
            duration: Duration(seconds: 1),
            curve: Curves.easeInOut,
            child: Container(
              width: 100,
              height: 100,
              color: Colors.blue,
              child: Center(
                child: Text(
                  'Move me!',
                  style: TextStyle(color: Colors.white),
                ),
              ),
            ),
          ),
          Positioned(
            bottom: 50,
            left: 50,
            child: ElevatedButton(
              onPressed: _moveWidget,
              child: Text('Move Widget'),
            ),
          )
        ],
      ),
    );
  }
}
